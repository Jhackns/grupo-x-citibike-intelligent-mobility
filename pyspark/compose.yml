name: lambda26-pyspark

services:
  pyspark:
    container_name: lambda26-pyspark
    build:
      context: .
    restart: unless-stopped
    ports:
      - "4487:8888"
      - "4043:4040"
    volumes:
      - ./sesiones:/opt
    environment:
      - JUPYTER_TOKEN=${JUPYTER_TOKEN}
